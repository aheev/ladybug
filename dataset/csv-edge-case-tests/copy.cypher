COPY `bom-and-data` FROM "bom-and-data.csv";
COPY `bom-and-header` FROM "bom-and-header.csv" (header = true);
COPY `bom` FROM "bom.csv";
COPY `carriage-return-then-eof` FROM "carriage-return-then-eof.csv";
COPY `delimiter-then-eof` FROM "delimiter-then-eof.csv";
COPY `empty-first-line` FROM "empty-first-line.csv";
COPY `empty-lines-multiple-columns` FROM "empty-lines-multiple-columns.csv";
COPY `empty-lines-single-column` FROM "empty-lines-single-column.csv";
COPY `empty` FROM "empty.csv";
COPY `empty-with-header` FROM "empty.csv";
COPY `eof-after-unquote` FROM "eof-after-unquote.csv";
COPY `escapes-in-quote` FROM "escapes-in-quote.csv";
COPY `escapes-out-of-quote` FROM "escapes-out-of-quote.csv" (sample_size=1);
COPY `mixed-empty-lines-multiple-columns` FROM "mixed-empty-lines-multiple-columns.csv";
COPY `mixed-empty-lines-single-column` FROM "mixed-empty-lines-single-column.csv";
COPY `mixed-newlines` FROM "mixed-newlines.csv";
COPY `quoted-values` FROM "quoted-values.csv";
COPY `tab-as-delim` FROM "tab-as-delim.csv";
COPY `unquote-escape` FROM "unquote-escape.csv";
